import CategoryWritePage from "@/components/CategoryWritePage"

export default function ReadingNotesWritePage() {
  return (
    <CategoryWritePage
      category="reading-notes"
      heading="Write: Reading Notes"
      description="Summaries, critiques, and interpretations of key texts and papers."
    />
  )
}
